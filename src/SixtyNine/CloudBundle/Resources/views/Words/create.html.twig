{% extends 'SixtyNineCloudBundle::layout.html.twig' %}

{% set curPage = 'words' %}
{% set pageTitle = 'Create list' | trans  %}
{% set pageHeader = 'List' | trans  %}
{% set pageSubHeader = 'Create' | trans  %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="pull-right">
        <a href="{{ path('sn_words_list') }}" class="btn btn-default">{{ 'Cancel' | trans }}</a>
        <input type="submit" class="btn btn-primary" value="{{ 'Create' | trans }}" />
    </div>
    {{ form_end(form) }}
{% endblock %}
